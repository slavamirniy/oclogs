import React from 'react';

function LogViewer({ activeTab }) {
  // Здесь может быть логика для отображения логов в зависимости от активной вкладки
  return (
    <div className="log-viewer">
      {/* Отображение логов для activeTab */}
    </div>
  );
}

export default LogViewer;